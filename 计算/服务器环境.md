# CREST/xTB 服务器环境

- 审计日期：2026-08-23（Asia/Shanghai）
- 主机名：`workstation`
- 调度器：Slurm；默认 CPU 分区 `192c`，最长运行时间 7 天
- 登录节点：Intel Xeon Gold 5218，64 逻辑 CPU，约 200 GiB 内存；登录环境未检测到 `nvidia-smi`
- 用户软件环境：`/home/zhanhao/software/quantum-cpu`
- xTB：6.7.1，commit `edcfbbe`
- CREST：3.0.2，commit `6914a25`
- Python：3.11.15
- RDKit：2024.09.2
- NumPy：2.4.6
- Pandas：3.0.5
- ORCA/r2SCAN-3c：未在 PATH 或用户软件目录发现；Tier-1b 暂停
- 远端项目根目录：`/home/zhanhao/TPU高通量筛选`
- 远端结果目录：`/home/zhanhao/TPU高通量筛选/计算/结果`

Slurm显示`SelectTypeParameters=NONE`，但运行队列证明同一节点可承载多个16–64核作业，不能按“整节点独占”解释。Job 8307最初申请128核，随后在保留Job ID和排队年龄的前提下降为32核；包装器按Slurm实际分配核数把CREST限制为8路并发，每路4线程。节点实际有约0.5–0.95 TiB空闲内存，但Slurm的`RealMemory`被登记为1 MiB，任何常规`--mem`请求都会被拒绝，因此脚本不声明内存资源；这是集群配置事实，不代表任务没有内存需求。本文不记录SSH密码。

- 已取消的初始数组烟雾任务：Job `8303`；取消原因是发现数组元素会整节点独占，任务尚未启动。
- 单节点烟雾后全量任务：Job `8307`；2026-08-23 15:19提交到`192c,256c`联合候选分区，初始原因为`PENDING (Priority)`。
- Job `8307`最终在`192c`节点运行`1-00:00:29`后达到24小时时限，Slurm终态为`TIMEOUT`。这不是账户或QoS限额错误。86个任务中83个严格完成、2个输入三维构象阻断、1个长链代理在最终优化约71%处被终止；该任务的中间`crest_conformers.xyz`不具备退出码0和最终哈希，不能进入正式发布。
- 任务84的恢复作业为Job `8519`：2026-08-25提交到`192c,256c`，申请16 CPU和48小时时限，提交后立即在`192c/cpu1`运行。脚本[恢复CREST任务84.sh](恢复CREST任务84.sh)通过原包装器创建`尝试_002`，不会覆盖8307留下的`尝试_001`；仍以退出码0、输入哈希和最终构象文件哈希作为完成门。
- 2026-08-23 15:23调度器给出的暂估启动时间为2026-08-27 19:17；该时间会随队列变化重算，不作为完成承诺。
- Slurm关联：账户/QoS正常；QoS每用户最多15个运行作业、30个提交作业，CPU上限1000、GPU上限1、节点上限20。Job 8307不是因配额限制排队。
