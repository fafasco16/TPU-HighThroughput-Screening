# TPU/TPUU 代理模型训练实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 训练三个带分组验证、不确定度和适用域判定的单任务代理模型，并为当前980条现实TPU配方输出通过门禁的预测或明确的阻断状态；不生成伪预测。

**Architecture:** 先把异构曲线和端点整理为“一行一个独立监督单元”的任务表，再将二异氰酸酯、宏二醇、扩链剂和计量条件转换为共享配方特征。计算数据仅用于表示预训练、低保真辅助或残差特征；实验主目标负责最终校准。模型按`leakage_group`和`source_family_id`分组验证，预测必须同时给出不确定度与适用域状态。

**Tech Stack:** Python 3.11、pandas、NumPy、RDKit、scikit-learn 1.9、CatBoost、Optuna、PyYAML、joblib、pytest、CSV/GZIP、SHA-256。

---

## 文件结构

**Create:**

- `代码/代理模型/__init__.py`：代理模型包公共版本号；
- `代码/代理模型/数据集.py`：标签标准化、独立单元聚合和任务矩阵；
- `代码/代理模型/特征.py`：构件描述符、指纹和配方聚合特征；
- `代码/代理模型/划分.py`：泄漏组、来源族和组分家族留出；
- `代码/代理模型/模型.py`：基线、CatBoost、ExtraTrees/HistGB和GPR集成；
- `代码/代理模型/评估.py`：加权指标、排序指标和区间覆盖率；
- `代码/代理模型/预测.py`：候选预测、适用域与三目标Pareto；
- `代码/训练代理模型.py`：统一CLI入口；
- `配置/代理模型.yaml`：目标、特征、模型和门禁配置；
- `代码/测试/test_代理模型数据集.py`；
- `代码/测试/test_代理模型特征.py`；
- `代码/测试/test_代理模型划分.py`；
- `代码/测试/test_代理模型训练.py`；
- `代码/测试/test_代理模型预测.py`；
- `结果/代理模型/README.md`；
- `结果/代理模型/训练集审计.csv`；
- `结果/代理模型/训练映射审计.csv`；
- `结果/代理模型/交叉验证结果.csv`；
- `结果/代理模型/候选预测.csv`；
- `结果/代理模型/模型卡.md`；
- `结果/代理模型/发布清单.json`。

**Modify:**

- `pyproject.toml`：增加`model`可选依赖；
- `uv.lock`：锁定模型依赖；
- `README.md`、`代码/README.md`、`结果/README.md`：加入训练与结果入口；
- `.gitignore`：明确模型权重、Optuna数据库和训练日志只保存在本地`模型/`。

**Local only:**

- `模型/代理模型_v0/`：CatBoost、joblib模型、特征模式和训练日志；该目录已被Git忽略。

---

### Task 1: 冻结训练规范与依赖

**Files:**

- Modify: `pyproject.toml`
- Modify: `uv.lock`
- Create: `配置/代理模型.yaml`
- Create: `代码/代理模型/__init__.py`
- Test: `代码/测试/test_代理模型数据集.py`

- [ ] **Step 1: 写依赖与配置失败测试**

```python
from pathlib import Path
import yaml


ROOT = Path(__file__).resolve().parents[2]


def test_proxy_model_configuration_is_explicit():
    config = yaml.safe_load(
        (ROOT / "配置" / "代理模型.yaml").read_text(encoding="utf-8")
    )
    assert set(config["objectives"]) == {
        "toughness",
        "cyclic_recovery",
        "thermal_stability",
    }
    assert config["candidate_scope"]["row_count"] == 980
    assert config["split"]["group_column"] == "leakage_group"
    assert config["split"]["source_holdout_column"] == "source_family_id"
    assert config["training"]["random_seed"] == 20260831
```

- [ ] **Step 2: 运行测试并确认配置尚不存在**

Run:

```powershell
uv run pytest 代码/测试/test_代理模型数据集.py::test_proxy_model_configuration_is_explicit -q
```

Expected: FAIL，提示`配置/代理模型.yaml`不存在。

- [ ] **Step 3: 增加模型依赖**

在`pyproject.toml`的`[project.optional-dependencies]`中加入：

```toml
model = [
  "scikit-learn>=1.9,<2",
  "catboost>=1.2,<2",
  "optuna>=4.9,<5",
  "joblib>=1.4,<2",
]
```

Run:

```powershell
uv lock
uv sync --extra dev --extra model
```

Expected: `uv.lock`更新，`python -c "import sklearn, catboost, optuna"`退出码为0。

- [ ] **Step 4: 写训练配置**

创建`配置/代理模型.yaml`：

```yaml
release_id: tpu-proxy-model-v0-2026-08-31
objectives:
  toughness:
    primary_head: toughness_MJ_m3
    source_properties: [toughness, tensile_toughness]
    auxiliary_properties: [tensile_strength, elongation_at_break]
    transform: log1p
    direction: maximize
  cyclic_recovery:
    primary_head: elastic_recovery_percent
    source_properties: [elastic_recovery]
    auxiliary_properties: [residual_strain, energy_dissipation_index]
    transform: identity
    direction: maximize
  thermal_stability:
    primary_head: T10_degC
    source_properties: []
    auxiliary_properties: [T5_degC, T50_degC, Td_onset_degC]
    transform: identity
    direction: maximize
candidate_scope:
  path: 结果/定向筛选/三目标配方特征.csv.gz
  row_count: 980
split:
  group_column: leakage_group
  source_holdout_column: source_family_id
  source_holdout_folds: 5
  development_splits: [train, validation, test]
training:
  random_seed: 20260831
  optuna_trials_per_model: 30
  sample_weight_column: recommended_loss_weight
  minimum_weight: 1.0e-8
prediction_gate:
  minimum_weighted_mae_improvement_fraction: 0.10
  minimum_median_spearman: 0.25
  nominal_interval_coverage: 0.80
  allowed_interval_coverage: [0.70, 0.90]
  minimum_core_groups_for_diagnostic_fit: 5
  minimum_core_groups_for_candidate_rank: 30
```

- [ ] **Step 5: 写包版本并运行测试**

```python
# 代码/代理模型/__init__.py
MODEL_RELEASE_ID = "tpu-proxy-model-v0-2026-08-31"
RANDOM_SEED = 20260831
```

Run:

```powershell
uv run pytest 代码/测试/test_代理模型数据集.py::test_proxy_model_configuration_is_explicit -q
```

Expected: PASS。

- [ ] **Step 6: 提交**

```powershell
git add pyproject.toml uv.lock 配置/代理模型.yaml 代码/代理模型/__init__.py 代码/测试/test_代理模型数据集.py
git commit -m "build: add proxy model dependencies and config"
```

---

### Task 2: 构建一行一个独立监督单元的训练表

**Files:**

- Create: `代码/代理模型/数据集.py`
- Modify: `配置/代理模型.yaml`
- Test: `代码/测试/test_代理模型数据集.py`

- [ ] **Step 1: 写防止曲线点冒充样本的失败测试**

```python
import pandas as pd
from 代理模型.数据集 import build_training_tables


def test_training_tables_are_independent_unit_level():
    tables = build_training_tables()
    assert set(tables) == {"toughness", "cyclic_recovery", "thermal_stability"}
    expected_heads = {
        "toughness": "toughness_MJ_m3",
        "cyclic_recovery": "elastic_recovery_percent",
        "thermal_stability": "T10_degC",
    }
    for objective, frame in tables.items():
        assert frame["training_row_id"].is_unique, objective
        assert frame["target_head"].eq(expected_heads[objective]).all(), objective
        assert frame["leakage_group"].notna().all(), objective
        assert frame["source_family_id"].notna().all(), objective
        assert frame["sample_weight"].gt(0).all(), objective
        assert frame["source_row_kind"].ne("raw_curve_point").all(), objective
        assert frame.groupby("training_row_id").size().max() == 1


def test_curve_points_are_aggregated_before_tabular_training():
    tables = build_training_tables()
    cyclic = tables["cyclic_recovery"]
    assert len(cyclic) < 106_138
    assert cyclic["source_row_count"].max() < 10_000
```

- [ ] **Step 2: 运行测试并确认模块不存在**

Run:

```powershell
uv run pytest 代码/测试/test_代理模型数据集.py -q
```

Expected: FAIL，提示`代理模型.数据集`无法导入。

- [ ] **Step 3: 实现统一读取与权重聚合**

在`代码/代理模型/数据集.py`中加入：

```python
from __future__ import annotations

import hashlib
import json
from pathlib import Path

import numpy as np
import pandas as pd
import yaml


ROOT = Path(__file__).resolve().parents[2]
LABELS = ROOT / "结果" / "定向筛选" / "三目标实验标签.csv.gz"
CONFIG = ROOT / "配置" / "代理模型.yaml"

LABEL_HEADS = {
    "toughness": {
        "head": "toughness_MJ_m3",
        "properties": {"toughness", "tensile_toughness"},
    },
    "cyclic_recovery": {
        "head": "elastic_recovery_percent",
        "properties": {"elastic_recovery"},
    },
    "thermal_stability": {
        "head": "T10_degC",
        "properties": set(),
    },
}


def _stable_id(*values: object) -> str:
    text = "|".join("" if pd.isna(value) else str(value) for value in values)
    return hashlib.sha256(text.encode("utf-8")).hexdigest()[:24]


def _weighted_mean(group: pd.DataFrame) -> float:
    values = pd.to_numeric(group["value"], errors="coerce").to_numpy(float)
    weights = pd.to_numeric(
        group["recommended_loss_weight"], errors="coerce"
    ).fillna(0).to_numpy(float)
    valid = np.isfinite(values) & np.isfinite(weights) & (weights > 0)
    if not valid.any():
        raise ValueError("独立单元没有正权重有限目标")
    return float(np.average(values[valid], weights=weights[valid]))


def _label_rows(labels: pd.DataFrame, objective: str) -> list[dict[str, object]]:
    rule = LABEL_HEADS[objective]
    selected = labels.loc[
        labels["target_family"].eq(objective)
        & labels["property_name"].isin(rule["properties"])
    ]
    rows = []
    keys = ["independent_unit", "property_name", "unit", "method_or_test_protocol"]
    for key, unit in selected.groupby(keys, dropna=False, sort=True):
        independent, property_name, value_unit, protocol = key
        first = unit.iloc[0]
        rows.append(
            {
                "training_row_id": _stable_id(objective, "label", *key),
                "objective_id": objective,
                "target_head": rule["head"],
                "target_value": _weighted_mean(unit),
                "target_unit": value_unit,
                "protocol": protocol,
                "formulation_id": first["formulation_id"],
                "independent_unit": independent,
                "leakage_group": first["leakage_group"],
                "source_family_id": first["source_family_id"],
                "development_split": first["development_split"],
                "sample_weight": float(unit["recommended_loss_weight"].sum()),
                "source_row_count": len(unit),
                "source_row_kind": "direct_scalar",
                "condition_json": "{}",
                "chemistry_mapping_status": first["chemistry_mapping_status"],
                "chemistry_json": "{}",
            }
        )
    return rows


def _endpoint_rows(config: dict, objective: str) -> list[dict[str, object]]:
    rows = []
    for spec in config["endpoint_sources"].get(objective, []):
        source = pd.read_csv(ROOT / spec["path"], low_memory=False)
        for row in source.itertuples(index=False):
            target = getattr(row, spec["target_column"])
            independent = getattr(row, spec["independent_column"])
            group = getattr(row, spec["group_column"])
            formulation = getattr(row, spec["formulation_column"])
            conditions = {
                column: getattr(row, column) for column in spec["condition_columns"]
            }
            chemistry = {
                column: getattr(row, column) for column in spec["feature_columns"]
            }
            if pd.isna(target):
                continue
            rows.append(
                {
                    "training_row_id": _stable_id(
                        objective,
                        spec["source_family_id"],
                        independent,
                        spec["target_head"],
                        json.dumps(conditions, sort_keys=True, default=str),
                    ),
                    "objective_id": objective,
                    "target_head": spec["target_head"],
                    "target_value": float(target),
                    "target_unit": spec["target_unit"],
                    "protocol": spec["protocol"],
                    "formulation_id": formulation,
                    "independent_unit": independent,
                    "leakage_group": group,
                    "source_family_id": spec["source_family_id"],
                    "development_split": spec["development_split"],
                    "sample_weight": float(spec["weight"]),
                    "source_row_count": 1,
                    "source_row_kind": "derived_endpoint",
                    "condition_json": json.dumps(
                        conditions, ensure_ascii=False, sort_keys=True, default=str
                    ),
                    "chemistry_mapping_status": spec["chemistry_mapping_status"],
                    "chemistry_json": json.dumps(
                        chemistry, ensure_ascii=False, sort_keys=True, default=str
                    ),
                }
            )
    return rows


def build_training_tables() -> dict[str, pd.DataFrame]:
    config = yaml.safe_load(CONFIG.read_text(encoding="utf-8"))
    labels = pd.read_csv(LABELS, low_memory=False)
    labels = labels.loc[
        labels["model_ready"].fillna(False)
        & pd.to_numeric(labels["recommended_loss_weight"], errors="coerce").gt(0)
    ].copy()
    output: dict[str, pd.DataFrame] = {}
    for objective in LABEL_HEADS:
        rows = _label_rows(labels, objective)
        rows.extend(_endpoint_rows(config, objective))
        table = pd.DataFrame(rows)
        if table.empty:
            raise ValueError(f"目标没有监督端点: {objective}")
        if table["training_row_id"].duplicated().any():
            raise ValueError(f"训练行ID重复: {objective}")
        output[objective] = table.sort_values("training_row_id").reset_index(drop=True)
    return output
```

- [ ] **Step 4: 增加端点包映射，不混合不等价指标**

在`配置/代理模型.yaml`中增加：

```yaml
endpoint_sources:
  toughness:
    - path: 结果/定向筛选/DRUM机械回收拉伸端点.csv
      target_column: toughness_MJ_m3
      target_head: toughness_MJ_m3
      target_unit: MJ/m3
      formulation_column: formulation_id
      independent_column: observation_id
      group_column: leakage_group
      condition_columns: [hard_segment_mass_fraction]
      feature_columns: [macrodiol_family, macrodiol_nominal_mn_g_mol, macrodiol_repeat_unit_text, diisocyanate_smiles, chain_extension_reagent]
      chemistry_mapping_status: component_topology_mapped_partial
      source_family_id: family_drum_tpuu_recycling_2024
      development_split: train
      protocol: Shimadzu_AGS-X_room_temperature_50mm_min
      weight: 1.0
    - path: 结果/定向筛选/QUB生物基自修复TPU拉伸端点.csv
      target_column: toughness_MJ_m3
      target_head: toughness_MJ_m3
      target_unit: MJ/m3
      formulation_column: formulation_id
      independent_column: curve_id
      group_column: split_group
      condition_columns: [condition, is_healed, hard_segment_wt_percent]
      feature_columns: [polyol_name, diisocyanate_smiles, chain_extender_name, hard_segment_wt_percent, NCO_OH_molar_ratio]
      chemistry_mapping_status: monomer_set_hard_segment_mapped
      source_family_id: family_qub_biobased_self_healing_tpu
      development_split: train
      protocol: source_tensile_protocol
      weight: 0.75
    - path: 结果/定向筛选/SHPU自愈拉伸端点.csv
      target_column: stress_strain_area_MJ_m3
      target_head: toughness_MJ_m3
      target_unit: MJ/m3
      formulation_column: formulation_id
      independent_column: source_locator
      group_column: split_group
      condition_columns: [healing_state, healing_time_h]
      feature_columns: []
      chemistry_mapping_status: source_material_code_only
      source_family_id: family_shpu_iontronic_adhesive
      development_split: train
      protocol: source_stress_strain_curve
      weight: 0.25
  cyclic_recovery:
    - path: 结果/定向筛选/TPUU循环端点.csv
      target_column: elastic_recovery_percent
      target_head: elastic_recovery_percent
      target_unit: percent
      formulation_column: formulation_id
      independent_column: sample_id
      group_column: curve_id
      condition_columns: [cycle_number, maximum_strain_percent]
      feature_columns: []
      chemistry_mapping_status: formulation_id_only
      source_family_id: family_drum_low_ceiling_tpuu
      development_split: train
      protocol: source_cyclic_tensile
      weight: 0.75
    - path: 结果/定向筛选/DRUM机械回收循环端点.csv
      target_column: elastic_recovery_percent
      target_head: elastic_recovery_percent
      target_unit: percent
      formulation_column: formulation_id
      independent_column: source_locator
      group_column: source_locator
      condition_columns: [cycle_number, maximum_strain_percent]
      feature_columns: [macrodiol_family, macrodiol_nominal_mn_g_mol, macrodiol_repeat_unit_text, diisocyanate_smiles, chain_extension_reagent]
      chemistry_mapping_status: component_topology_mapped_partial
      source_family_id: family_drum_tpuu_recycling_2024
      development_split: train
      protocol: source_hysteresis_test
      weight: 1.0
  thermal_stability:
    - path: 结果/定向筛选/DRUM机械回收TGA端点.csv
      target_column: T10_degC
      target_head: T10_degC
      target_unit: degC
      formulation_column: formulation_id
      independent_column: source_locator
      group_column: formulation_id
      condition_columns: [hard_segment_mass_fraction]
      feature_columns: [macrodiol_family, macrodiol_nominal_mn_g_mol, macrodiol_repeat_unit_text, diisocyanate_smiles, chain_extension_reagent]
      chemistry_mapping_status: component_topology_mapped_partial
      source_family_id: family_drum_tpuu_recycling_2024
      development_split: train
      protocol: source_TGA
      weight: 1.0
    - path: 结果/定向筛选/低天花板TPUU热稳定端点.csv
      target_column: T10_degC
      target_head: T10_degC
      target_unit: degC
      formulation_column: formulation_id
      independent_column: sample_id
      group_column: split_group
      condition_columns: []
      feature_columns: []
      chemistry_mapping_status: formulation_id_only
      source_family_id: family_drum_low_ceiling_tpuu
      development_split: train
      protocol: source_TGA
      weight: 1.0
    - path: 结果/定向筛选/QUB生物基自修复TPUTGA端点.csv
      target_column: T10_C
      target_head: T10_degC
      target_unit: degC
      formulation_column: formulation_id
      independent_column: curve_id
      group_column: split_group
      condition_columns: [heating_rate_C_min, atmosphere]
      feature_columns: [polyol_name, diisocyanate_smiles, chain_extender_name, hard_segment_wt_percent, NCO_OH_molar_ratio]
      chemistry_mapping_status: monomer_set_hard_segment_mapped
      source_family_id: family_qub_biobased_self_healing_tpu
      development_split: train
      protocol: source_TGA
      weight: 0.75
    - path: 结果/定向筛选/TPU鞋材TGA端点.csv
      target_column: T10_degC
      target_head: T10_degC
      target_unit: degC
      formulation_column: formulation_id
      independent_column: source_locator
      group_column: split_group
      condition_columns: [heating_rate_degC_min]
      feature_columns: [material_family, supplier, hardness_shore_A, density_g_cm3]
      chemistry_mapping_status: commercial_grade_identity_only
      source_family_id: family_tpu_footwear_elastomers
      development_split: test
      protocol: nitrogen_TGA
      weight: 0.50
```

实现规则由上述`_endpoint_rows`读取：每个文件只读取配置声明的目标列和身份列；Tg、热导率、部分曲线面积、界面韧性以及IIR-OH峰值应力保持率不得转成主目标。

- [ ] **Step 5: 运行测试并检查数量审计**

Run:

```powershell
uv run pytest 代码/测试/test_代理模型数据集.py -q
```

Expected: PASS；循环训练表行数小于106,138，且每行对应一个独立单元/指标/协议。

- [ ] **Step 6: 提交**

```powershell
git add 配置/代理模型.yaml 代码/代理模型/数据集.py 代码/测试/test_代理模型数据集.py
git commit -m "feat: build leakage-safe proxy training tables"
```

---

### Task 3: 生成构件和配方特征

**Files:**

- Create: `代码/代理模型/特征.py`
- Test: `代码/测试/test_代理模型特征.py`

- [ ] **Step 1: 写特征一致性失败测试**

```python
import numpy as np
from 代理模型.数据集 import build_training_tables
from 代理模型.特征 import (
    build_candidate_features,
    build_training_feature_audit,
    molecular_descriptors,
)


def test_molecular_descriptors_are_deterministic():
    first = molecular_descriptors("CCO")
    second = molecular_descriptors("CCO")
    assert first == second
    assert first["MolWt"] > 40
    assert first["MorganBitCount"] > 0


def test_all_980_candidates_receive_feature_rows():
    features = build_candidate_features()
    assert len(features) == 980
    assert features["formulation_id"].is_unique
    assert np.isfinite(features.select_dtypes("number")).all().all()
    assert features["macrodiol_identity_mode"].isin(
        ["exact_smiles", "repeat_unit_and_grade"]
    ).all()


def test_incomplete_training_chemistry_is_never_imputed_as_zero():
    audit = build_training_feature_audit(build_training_tables())
    assert set(audit["feature_join_status"]) <= {
        "complete_candidate_schema",
        "partial_transfer_schema",
        "source_code_only",
    }
    incomplete = audit["feature_join_status"].ne("complete_candidate_schema")
    assert audit.loc[incomplete, "candidate_model_eligible"].eq(False).all()
```

- [ ] **Step 2: 运行测试并确认失败**

Run:

```powershell
uv run pytest 代码/测试/test_代理模型特征.py -q
```

Expected: FAIL，提示`代理模型.特征`不存在。

- [ ] **Step 3: 实现分子描述符与Morgan指纹摘要**

```python
from __future__ import annotations

import json
from functools import lru_cache
from pathlib import Path

import numpy as np
import pandas as pd
from rdkit import Chem
from rdkit.Chem import Descriptors, Lipinski, rdFingerprintGenerator, rdMolDescriptors


ROOT = Path(__file__).resolve().parents[2]
CANDIDATES = ROOT / "结果" / "定向筛选" / "三目标配方特征.csv.gz"
COMPONENTS = ROOT / "结果" / "定向筛选" / "现实构件约束.csv"


@lru_cache(maxsize=4096)
def molecular_descriptors(smiles: str) -> dict[str, float]:
    mol = Chem.MolFromSmiles(smiles)
    if mol is None:
        raise ValueError(f"无效SMILES: {smiles}")
    generator = rdFingerprintGenerator.GetMorganGenerator(radius=2, fpSize=1024)
    fingerprint = generator.GetFingerprint(mol)
    return {
        "MolWt": float(Descriptors.MolWt(mol)),
        "TPSA": float(rdMolDescriptors.CalcTPSA(mol)),
        "LogP": float(Descriptors.MolLogP(mol)),
        "HBD": float(Lipinski.NumHDonors(mol)),
        "HBA": float(Lipinski.NumHAcceptors(mol)),
        "RingCount": float(Lipinski.RingCount(mol)),
        "RotatableBonds": float(Lipinski.NumRotatableBonds(mol)),
        "FractionCSP3": float(rdMolDescriptors.CalcFractionCSP3(mol)),
        "MorganBitCount": float(fingerprint.GetNumOnBits()),
    }
```

- [ ] **Step 4: 实现宏二醇与配方聚合特征**

```python
def _component_features(prefix: str, smiles: object) -> dict[str, float]:
    if pd.isna(smiles) or not str(smiles).strip():
        return {f"{prefix}_{name}": 0.0 for name in molecular_descriptors("CC")} | {
            f"{prefix}_exact_smiles_available": 0.0
        }
    return {
        f"{prefix}_{name}": value
        for name, value in molecular_descriptors(str(smiles)).items()
    } | {f"{prefix}_exact_smiles_available": 1.0}


def build_candidate_features() -> pd.DataFrame:
    candidates = pd.read_csv(CANDIDATES, low_memory=False)
    rows = []
    for row in candidates.itertuples(index=False):
        features = {
            "formulation_id": row.formulation_id,
            "macrodiol_family": row.macrodiol_name,
            "macrodiol_identity_mode": (
                "exact_smiles" if pd.notna(row.macrodiol_smiles) else "repeat_unit_and_grade"
            ),
            "macrodiol_mn": float(row.macrodiol_nominal_mn_g_mol),
            "hard_segment_fraction": float(row.hard_segment_mass_fraction_target),
            "nco_oh_ratio": float(row.nco_oh_ratio_target),
        }
        features.update(_component_features("iso", row.diisocyanate_smiles))
        features.update(_component_features("ext", row.chain_extender_smiles))
        rows.append(features)
    return pd.DataFrame(rows)


def build_training_feature_audit(
    tables: dict[str, pd.DataFrame],
) -> pd.DataFrame:
    components = pd.read_csv(COMPONENTS, low_memory=False)
    commercial_smiles = set(
        components["canonical_smiles"].dropna().astype(str)
    )
    commercial_names = set(components["preferred_name"].dropna().astype(str))
    rows = []
    for objective, table in tables.items():
        for row in table.itertuples(index=False):
            chemistry = json.loads(row.chemistry_json)
            has_iso = bool(chemistry.get("diisocyanate_smiles"))
            has_macro = bool(
                chemistry.get("macrodiol_repeat_unit_text")
                or chemistry.get("polyol_name")
            )
            has_extender = bool(
                chemistry.get("chain_extender_smiles")
                or chemistry.get("chain_extender_name")
            )
            iso_in_scope = chemistry.get("diisocyanate_smiles") in commercial_smiles
            macro_in_scope = chemistry.get("polyol_name") in commercial_names or chemistry.get(
                "macrodiol_family"
            ) in commercial_names
            extender_in_scope = chemistry.get("chain_extender_name") in commercial_names
            complete = (
                has_iso
                and has_macro
                and has_extender
                and iso_in_scope
                and macro_in_scope
                and extender_in_scope
            )
            partial = any((has_iso, has_macro, has_extender))
            status = (
                "complete_candidate_schema"
                if complete
                else "partial_transfer_schema"
                if partial
                else "source_code_only"
            )
            rows.append(
                {
                    "objective_id": objective,
                    "training_row_id": row.training_row_id,
                    "formulation_id": row.formulation_id,
                    "feature_join_status": status,
                    "candidate_model_eligible": complete,
                    "has_diisocyanate_structure": has_iso,
                    "has_macrodiol_context": has_macro,
                    "has_chain_extender_context": has_extender,
                    "diisocyanate_in_candidate_scope": iso_in_scope,
                    "macrodiol_in_candidate_scope": macro_in_scope,
                    "chain_extender_in_candidate_scope": extender_in_scope,
                }
            )
    return pd.DataFrame(rows)


def build_diagnostic_features(table: pd.DataFrame) -> pd.DataFrame:
    conditions = pd.json_normalize(
        table["condition_json"].map(json.loads)
    ).add_prefix("condition_")
    numeric_conditions = conditions.apply(pd.to_numeric, errors="coerce").fillna(0.0)
    categorical = table[
        ["formulation_id", "source_family_id", "protocol", "target_head"]
    ].fillna("missing").reset_index(drop=True)
    return pd.concat([categorical, numeric_conditions.reset_index(drop=True)], axis=1)


def build_candidate_training_features(
    table: pd.DataFrame,
) -> tuple[pd.DataFrame, pd.DataFrame]:
    if table.empty:
        return pd.DataFrame(), table.copy()
    audit = build_training_feature_audit({table.iloc[0]["objective_id"]: table})
    eligible_ids = set(
        audit.loc[audit["candidate_model_eligible"], "training_row_id"]
    )
    selected = table.loc[table["training_row_id"].isin(eligible_ids)].copy()
    components = pd.read_csv(COMPONENTS, low_memory=False)
    by_name = components.set_index("preferred_name", drop=False)
    rows = []
    for row in selected.itertuples(index=False):
        chemistry = json.loads(row.chemistry_json)
        conditions = json.loads(row.condition_json)
        iso_smiles = chemistry["diisocyanate_smiles"]
        extender_name = chemistry.get("chain_extender_name")
        extender_smiles = by_name.loc[extender_name, "canonical_smiles"]
        macro_name = chemistry.get("polyol_name") or chemistry.get("macrodiol_family")
        features = {
            "training_row_id": row.training_row_id,
            "macrodiol_family": macro_name,
            "macrodiol_identity_mode": "repeat_unit_and_grade",
            "macrodiol_mn": float(
                chemistry.get("macrodiol_nominal_mn_g_mol", 0.0)
            ),
            "hard_segment_fraction": float(
                conditions.get("hard_segment_mass_fraction", 0.0)
            ),
            "nco_oh_ratio": float(conditions.get("NCO_OH_molar_ratio", 1.0)),
        }
        features.update(_component_features("iso", iso_smiles))
        features.update(_component_features("ext", extender_smiles))
        rows.append(features)
    return pd.DataFrame(rows), selected.reset_index(drop=True)
```

- [ ] **Step 5: 运行测试**

Run:

```powershell
uv run pytest 代码/测试/test_代理模型特征.py -q
```

Expected: PASS，980个候选均有唯一、有限的特征行；训练数据缺失化学映射时明确隔离，不以0填充冒充真实构件。

- [ ] **Step 6: 写训练映射审计**

```python
audit = build_training_feature_audit(build_training_tables())
audit.to_csv(
    ROOT / "结果" / "代理模型" / "训练映射审计.csv",
    index=False,
    encoding="utf-8-sig",
    lineterminator="\n",
)
```

每个目标统计`complete_candidate_schema`对应的唯一泄漏组；不足5组时只允许训练来源/协议诊断模型，不训练候选化学模型；不足30组时即使诊断模型通过，也不允许发布正式候选排名。

- [ ] **Step 7: 提交**

```powershell
git add 代码/代理模型/特征.py 代码/测试/test_代理模型特征.py 结果/代理模型/训练映射审计.csv
git commit -m "feat: generate component and formulation descriptors"
```

---

### Task 4: 实现防泄漏划分和基线门

**Files:**

- Create: `代码/代理模型/划分.py`
- Create: `代码/代理模型/评估.py`
- Test: `代码/测试/test_代理模型划分.py`

- [ ] **Step 1: 写泄漏检测失败测试**

```python
import pandas as pd
from 代理模型.划分 import assert_no_group_leakage, source_holdout_splits


def test_source_holdout_never_splits_leakage_group():
    frame = pd.DataFrame(
        {
            "source_family_id": ["A", "A", "B", "C"],
            "leakage_group": ["g1", "g1", "g2", "g3"],
            "target_value": [1.0, 1.1, 2.0, 3.0],
        }
    )
    splits = list(source_holdout_splits(frame, n_splits=3))
    assert len(splits) == 3
    for train_index, test_index in splits:
        assert_no_group_leakage(frame, train_index, test_index)
```

- [ ] **Step 2: 运行测试并确认失败**

Run:

```powershell
uv run pytest 代码/测试/test_代理模型划分.py -q
```

Expected: FAIL，提示`代理模型.划分`不存在。

- [ ] **Step 3: 实现来源族留出**

```python
import numpy as np
import pandas as pd


def source_holdout_splits(frame: pd.DataFrame, n_splits: int = 5):
    sources = sorted(frame["source_family_id"].dropna().astype(str).unique())
    if len(sources) < 2:
        raise ValueError("来源族不足，无法外推验证")
    fold_count = min(n_splits, len(sources))
    for fold in range(fold_count):
        test_sources = set(sources[fold::fold_count])
        test_mask = frame["source_family_id"].astype(str).isin(test_sources)
        yield np.flatnonzero(~test_mask.to_numpy()), np.flatnonzero(test_mask.to_numpy())


def assert_no_group_leakage(frame, train_index, test_index) -> None:
    train_groups = set(frame.iloc[train_index]["leakage_group"].astype(str))
    test_groups = set(frame.iloc[test_index]["leakage_group"].astype(str))
    overlap = train_groups & test_groups
    if overlap:
        raise AssertionError(f"发现泄漏组跨折: {sorted(overlap)[:5]}")
```

- [ ] **Step 4: 实现加权基线和评价指标**

```python
import numpy as np
import pandas as pd
from scipy.stats import spearmanr
from sklearn.metrics import mean_absolute_error, mean_squared_error


def weighted_median(values, weights):
    order = np.argsort(values)
    values = np.asarray(values)[order]
    weights = np.asarray(weights)[order]
    cutoff = weights.sum() / 2
    return float(values[np.searchsorted(np.cumsum(weights), cutoff)])


def regression_metrics(y_true, y_pred, weights):
    return {
        "weighted_mae": float(mean_absolute_error(y_true, y_pred, sample_weight=weights)),
        "weighted_rmse": float(
            mean_squared_error(y_true, y_pred, sample_weight=weights) ** 0.5
        ),
        "spearman": float(spearmanr(y_true, y_pred).statistic),
    }
```

- [ ] **Step 5: 运行测试与提交**

Run:

```powershell
uv run pytest 代码/测试/test_代理模型划分.py -q
```

Expected: PASS。

```powershell
git add 代码/代理模型/划分.py 代码/代理模型/评估.py 代码/测试/test_代理模型划分.py
git commit -m "feat: add leakage-safe proxy validation"
```

---

### Task 5: 训练三目标基线与集成模型

**Files:**

- Create: `代码/代理模型/模型.py`
- Create: `代码/训练代理模型.py`
- Test: `代码/测试/test_代理模型训练.py`

- [ ] **Step 1: 写模型契约失败测试**

```python
import numpy as np
import pandas as pd
from 代理模型.模型 import fit_ensemble


def test_ensemble_returns_mean_and_uncertainty():
    X = pd.DataFrame(
        {"numeric": np.arange(20, dtype=float), "family": ["A", "B"] * 10}
    )
    y = np.arange(20, dtype=float) * 0.5
    weights = np.ones(20)
    ensemble = fit_ensemble(X, y, weights, categorical=["family"], random_seed=7)
    prediction = ensemble.predict(X.iloc[:3])
    assert set(prediction) == {"mean", "std", "member_predictions"}
    assert prediction["mean"].shape == (3,)
    assert np.all(prediction["std"] >= 0)
```

- [ ] **Step 2: 运行测试并确认失败**

Run:

```powershell
uv run pytest 代码/测试/test_代理模型训练.py -q
```

Expected: FAIL，提示`fit_ensemble`不存在。

- [ ] **Step 3: 实现CatBoost与HistGB集成**

```python
from dataclasses import dataclass

import numpy as np
import pandas as pd
from catboost import CatBoostRegressor
from sklearn.compose import ColumnTransformer
from sklearn.ensemble import HistGradientBoostingRegressor
from sklearn.pipeline import Pipeline
from sklearn.preprocessing import OneHotEncoder


@dataclass
class Ensemble:
    members: list

    def predict(self, X: pd.DataFrame):
        values = np.vstack([member.predict(X) for member in self.members])
        return {
            "mean": values.mean(axis=0),
            "std": values.std(axis=0, ddof=1),
            "member_predictions": values,
        }


def fit_ensemble(X, y, weights, categorical, random_seed):
    categorical = list(categorical)
    numeric = [column for column in X.columns if column not in categorical]
    catboost = CatBoostRegressor(
        loss_function="RMSE",
        iterations=400,
        depth=5,
        learning_rate=0.03,
        l2_leaf_reg=5,
        random_seed=random_seed,
        verbose=False,
        allow_writing_files=False,
    )
    catboost.fit(X, y, cat_features=categorical, sample_weight=weights)
    preprocessor = ColumnTransformer(
        [
            ("categorical", OneHotEncoder(handle_unknown="ignore", sparse_output=False), categorical),
            ("numeric", "passthrough", numeric),
        ]
    )
    hist = Pipeline(
        [
            ("preprocess", preprocessor),
            (
                "model",
                HistGradientBoostingRegressor(
                    max_iter=300,
                    max_leaf_nodes=15,
                    learning_rate=0.04,
                    l2_regularization=2.0,
                    random_state=random_seed,
                ),
            ),
        ]
    )
    hist.fit(X, y, model__sample_weight=weights)
    return Ensemble([catboost, hist])
```

- [ ] **Step 4: 实现统一CLI**

```python
from __future__ import annotations

import argparse
import json
from pathlib import Path

import joblib
import numpy as np

from 代理模型.数据集 import build_training_tables
from 代理模型.特征 import (
    build_candidate_training_features,
    build_diagnostic_features,
)
from 代理模型.模型 import fit_ensemble


ROOT = Path(__file__).resolve().parents[1]


def main():
    parser = argparse.ArgumentParser()
    parser.add_argument("--objective", choices=["toughness", "cyclic_recovery", "thermal_stability"], required=True)
    parser.add_argument("--dry-run", action="store_true")
    args = parser.parse_args()
    frame = build_training_tables()[args.objective]
    summary = {
        "objective": args.objective,
        "rows": len(frame),
        "independent_units": int(frame["independent_unit"].nunique()),
        "sources": int(frame["source_family_id"].nunique()),
    }
    candidate_X, candidate_rows = build_candidate_training_features(frame)
    summary["candidate_schema_rows"] = len(candidate_rows)
    summary["candidate_schema_groups"] = int(
        candidate_rows["leakage_group"].nunique()
    )
    if args.dry_run:
        print(json.dumps(summary, ensure_ascii=False))
        return
    output = ROOT / "模型" / "代理模型_v0" / args.objective
    output.mkdir(parents=True, exist_ok=True)
    diagnostic_X = build_diagnostic_features(frame)
    target = frame["target_value"].to_numpy(float)
    transformed_target = (
        np.log1p(np.maximum(target, 0.0))
        if args.objective == "toughness"
        else target
    )
    weights = frame["sample_weight"].to_numpy(float)
    diagnostic_model = fit_ensemble(
        diagnostic_X,
        transformed_target,
        weights,
        categorical=["formulation_id", "source_family_id", "protocol", "target_head"],
        random_seed=20260831,
    )
    artifact = {
        "summary": summary,
        "target_transform": "log1p" if args.objective == "toughness" else "identity",
        "diagnostic_model": diagnostic_model,
        "candidate_model": None,
    }
    if summary["candidate_schema_groups"] >= 5:
        candidate_y = candidate_rows["target_value"].to_numpy(float)
        if args.objective == "toughness":
            candidate_y = np.log1p(np.maximum(candidate_y, 0.0))
        artifact["candidate_model"] = fit_ensemble(
            candidate_X.drop(columns="training_row_id"),
            candidate_y,
            candidate_rows["sample_weight"].to_numpy(float),
            categorical=["macrodiol_family", "macrodiol_identity_mode"],
            random_seed=20260831,
        )
    joblib.dump(artifact, output / "model_bundle.joblib")
    (output / "training_summary.json").write_text(
        json.dumps(summary, ensure_ascii=False, indent=2) + "\n",
        encoding="utf-8",
    )
```

- [ ] **Step 5: 运行单元测试和dry-run**

Run:

```powershell
uv run pytest 代码/测试/test_代理模型训练.py -q
uv run python 代码/训练代理模型.py --objective toughness --dry-run
uv run python 代码/训练代理模型.py --objective cyclic_recovery --dry-run
uv run python 代码/训练代理模型.py --objective thermal_stability --dry-run
```

Expected: 测试PASS；三个dry-run输出正行数、独立单元和来源数，不写模型权重。

- [ ] **Step 6: 提交**

```powershell
git add 代码/代理模型/模型.py 代码/训练代理模型.py 代码/测试/test_代理模型训练.py
git commit -m "feat: add three-target proxy ensemble"
```

---

### Task 6: 分组交叉验证、门禁和不确定度

**Files:**

- Modify: `代码/代理模型/模型.py`
- Modify: `代码/代理模型/评估.py`
- Modify: `代码/训练代理模型.py`
- Test: `代码/测试/test_代理模型训练.py`

- [ ] **Step 1: 写发布门失败测试**

```python
from 代理模型.评估 import evaluate_prediction_gate


def test_prediction_gate_rejects_model_worse_than_baseline():
    decision = evaluate_prediction_gate(
        model_mae=1.0,
        baseline_mae=0.9,
        median_spearman=0.4,
        interval_coverage=0.8,
        candidate_schema_groups=40,
    )
    assert decision["diagnostic_passed"] is False
    assert "mae_improvement" in decision["failed_checks"]


def test_prediction_gate_accepts_calibrated_ranker():
    decision = evaluate_prediction_gate(
        model_mae=0.7,
        baseline_mae=1.0,
        median_spearman=0.5,
        interval_coverage=0.8,
        candidate_schema_groups=35,
    )
    assert decision["diagnostic_passed"] is True
    assert decision["candidate_rank_publishable"] is True


def test_small_chemistry_overlap_allows_diagnostic_only():
    decision = evaluate_prediction_gate(
        model_mae=0.7,
        baseline_mae=1.0,
        median_spearman=0.5,
        interval_coverage=0.8,
        candidate_schema_groups=8,
    )
    assert decision["diagnostic_passed"] is True
    assert decision["candidate_rank_publishable"] is False
```

- [ ] **Step 2: 实现门禁**

```python
def evaluate_prediction_gate(
    *, model_mae, baseline_mae, median_spearman, interval_coverage,
    candidate_schema_groups
):
    improvement = (baseline_mae - model_mae) / max(baseline_mae, 1e-12)
    checks = {
        "mae_improvement": improvement >= 0.10,
        "rank_correlation": median_spearman >= 0.25,
        "interval_coverage": 0.70 <= interval_coverage <= 0.90,
        "diagnostic_chemistry_overlap": candidate_schema_groups >= 5,
    }
    diagnostic_passed = all(checks.values()) and candidate_schema_groups >= 5
    return {
        "diagnostic_passed": diagnostic_passed,
        "candidate_rank_publishable": (
            diagnostic_passed and candidate_schema_groups >= 30
        ),
        "checks": checks,
        "failed_checks": [name for name, passed in checks.items() if not passed],
        "mae_improvement_fraction": improvement,
        "candidate_schema_groups": candidate_schema_groups,
        "candidate_rank_block_reason": (
            "candidate_schema_groups_below_30"
            if candidate_schema_groups < 30
            else ""
        ),
    }
```

- [ ] **Step 3: 实现来源族交叉验证输出**

交叉验证每折必须记录：目标、折号、测试来源族、训练/测试独立单元数、加权MAE/RMSE、Spearman、基线MAE、区间覆盖率和门禁结果。输出列固定为：

```python
CV_COLUMNS = [
    "objective_id", "fold", "test_source_families",
    "train_independent_units", "test_independent_units",
    "weighted_mae", "weighted_rmse", "spearman",
    "baseline_weighted_mae", "interval_coverage", "gate_passed",
]


def cross_validate_model(frame, X, categorical, random_seed=20260831):
    from 代理模型.划分 import assert_no_group_leakage, source_holdout_splits
    from 代理模型.模型 import fit_ensemble

    rows = []
    y = frame["target_value"].to_numpy(float)
    weights = frame["sample_weight"].to_numpy(float)
    for fold, (train_index, test_index) in enumerate(
        source_holdout_splits(frame), start=1
    ):
        assert_no_group_leakage(frame, train_index, test_index)
        model = fit_ensemble(
            X.iloc[train_index],
            y[train_index],
            weights[train_index],
            categorical=categorical,
            random_seed=random_seed + fold,
        )
        prediction = model.predict(X.iloc[test_index])
        baseline_value = weighted_median(y[train_index], weights[train_index])
        baseline_prediction = np.full(len(test_index), baseline_value)
        metrics = regression_metrics(
            y[test_index], prediction["mean"], weights[test_index]
        )
        baseline = regression_metrics(
            y[test_index], baseline_prediction, weights[test_index]
        )
        half_width = 1.281552 * np.maximum(prediction["std"], 1e-12)
        coverage = np.average(
            (y[test_index] >= prediction["mean"] - half_width)
            & (y[test_index] <= prediction["mean"] + half_width),
            weights=weights[test_index],
        )
        rows.append(
            {
                "fold": fold,
                "test_source_families": ";".join(
                    sorted(frame.iloc[test_index]["source_family_id"].unique())
                ),
                "train_independent_units": int(
                    frame.iloc[train_index]["independent_unit"].nunique()
                ),
                "test_independent_units": int(
                    frame.iloc[test_index]["independent_unit"].nunique()
                ),
                "weighted_mae": metrics["weighted_mae"],
                "weighted_rmse": metrics["weighted_rmse"],
                "spearman": metrics["spearman"],
                "baseline_weighted_mae": baseline["weighted_mae"],
                "interval_coverage": float(coverage),
            }
        )
    return pd.DataFrame(rows)
```

- [ ] **Step 4: 热稳定特殊门**

若`thermal_stability`的核心化学闭合组仍为0，则：

```python
candidate_prediction_status = "auxiliary_proxy_only_no_core_chemistry_calibration"
candidate_rank_publishable = False
```

热稳定模型仍训练用于方法验证和未来增量校准，但不得进入正式三目标Pareto。

- [ ] **Step 5: 运行测试与提交**

```powershell
uv run pytest 代码/测试/test_代理模型训练.py -q
git add 代码/代理模型/模型.py 代码/代理模型/评估.py 代码/训练代理模型.py 代码/测试/test_代理模型训练.py
git commit -m "feat: add grouped proxy evaluation gates"
```

---

### Task 7: 候选预测、适用域和Pareto排序

**Files:**

- Create: `代码/代理模型/预测.py`
- Modify: `代码/训练代理模型.py`
- Test: `代码/测试/test_代理模型预测.py`

- [ ] **Step 1: 写Pareto与适用域失败测试**

```python
import pandas as pd
from 代理模型.预测 import pareto_rank, applicability_status


def test_pareto_rank_marks_dominated_candidate():
    frame = pd.DataFrame(
        {
            "toughness": [10.0, 8.0, 12.0],
            "recovery": [80.0, 70.0, 75.0],
            "thermal": [300.0, 280.0, 290.0],
        }
    )
    ranks = pareto_rank(frame, ["toughness", "recovery", "thermal"])
    assert ranks.tolist() == [1, 2, 1]


def test_applicability_requires_seen_component_families():
    status = applicability_status(
        min_component_similarity=0.82,
        categorical_coverage=True,
        descriptor_distance=1.4,
    )
    assert status == "in_domain"
    assert applicability_status(0.42, True, 1.4) == "out_of_domain"
```

- [ ] **Step 2: 实现非支配排序**

```python
import numpy as np
import pandas as pd


def pareto_rank(frame: pd.DataFrame, columns: list[str]) -> pd.Series:
    values = frame[columns].to_numpy(float)
    remaining = set(range(len(frame)))
    ranks = np.zeros(len(frame), dtype=int)
    rank = 1
    while remaining:
        front = []
        for index in remaining:
            dominated = any(
                np.all(values[other] >= values[index])
                and np.any(values[other] > values[index])
                for other in remaining
                if other != index
            )
            if not dominated:
                front.append(index)
        for index in front:
            ranks[index] = rank
            remaining.remove(index)
        rank += 1
    return pd.Series(ranks, index=frame.index, name="pareto_rank")
```

- [ ] **Step 3: 实现适用域**

```python
def applicability_status(
    min_component_similarity: float,
    categorical_coverage: bool,
    descriptor_distance: float,
) -> str:
    if not categorical_coverage or min_component_similarity < 0.60:
        return "out_of_domain"
    if min_component_similarity < 0.75 or descriptor_distance > 3.0:
        return "borderline"
    return "in_domain"
```

- [ ] **Step 4: 规定候选预测字段**

`结果/代理模型/候选预测.csv`必须包含：

```python
PREDICTION_COLUMNS = [
    "formulation_id",
    "toughness_mean", "toughness_std", "toughness_status",
    "cyclic_recovery_mean", "cyclic_recovery_std", "cyclic_recovery_status",
    "thermal_stability_mean", "thermal_stability_std", "thermal_stability_status",
    "minimum_component_similarity", "descriptor_distance",
    "applicability_status", "pareto_rank", "screening_decision",
]
```

只有韧性和循环恢复的`candidate_rank_publishable=true`且适用域不是`out_of_domain`时，`screening_decision`才可为`advance_to_low_cost_calculation`。热稳定未通过核心门时只显示辅助值，不参与Pareto；任一候选化学模型因映射覆盖不足而被阻断时，对应均值和标准差留空。

- [ ] **Step 5: 运行测试与提交**

```powershell
uv run pytest 代码/测试/test_代理模型预测.py -q
git add 代码/代理模型/预测.py 代码/训练代理模型.py 代码/测试/test_代理模型预测.py
git commit -m "feat: add proxy candidate ranking and applicability"
```

---

### Task 8: 训练、发布模型卡和最终验收

**Files:**

- Modify: `代码/训练代理模型.py`
- Create: `结果/代理模型/README.md`
- Create: `结果/代理模型/训练集审计.csv`
- Create: `结果/代理模型/交叉验证结果.csv`
- Create: `结果/代理模型/候选预测.csv`
- Create: `结果/代理模型/模型卡.md`
- Create: `结果/代理模型/发布清单.json`
- Modify: `README.md`
- Modify: `代码/README.md`
- Modify: `结果/README.md`

- [ ] **Step 1: 执行三目标训练**

```powershell
uv run python 代码/训练代理模型.py --objective toughness
uv run python 代码/训练代理模型.py --objective cyclic_recovery
uv run python 代码/训练代理模型.py --objective thermal_stability
```

Expected: 模型权重写入本地`模型/代理模型_v0/`；Git跟踪区不出现`.cbm`、`.joblib`或Optuna数据库。

- [ ] **Step 2: 执行候选预测**

```powershell
uv run python 代码/训练代理模型.py --predict-candidates
```

Expected: `结果/代理模型/候选预测.csv`恰好980行，`formulation_id`唯一；不满足门禁的目标状态明确，不填充伪预测。

- [ ] **Step 3: 写模型卡**

`结果/代理模型/模型卡.md`必须逐目标记录：

```markdown
## 韧性
- 目标定义：完整应力—应变积分，单位MJ/m³
- 主目标独立单元：以训练集审计为准
- 辅助数据：强度、伸长和迁移曲线面积
- 验证：来源族留出＋泄漏组检查
- 门禁：加权MAE相对基线改善≥10%，Spearman≥0.25
- 禁止解释：迁移层结果不等于核心TPU实验真值
```

循环恢复和热稳定也必须完整填写上述六项字段。热稳定核心闭合为0时必须写明不能进入正式候选Pareto。

- [ ] **Step 4: 写发布清单**

`结果/代理模型/发布清单.json`至少包含：

```json
{
  "release_id": "tpu-proxy-model-v0-2026-08-31",
  "random_seed": 20260831,
  "candidate_count": 980,
  "model_weights_tracked_by_git": false,
  "input_hashes": {},
  "output_hashes": {},
  "objective_gate_status": {}
}
```

- [ ] **Step 5: 全量验证**

Run:

```powershell
uv run pytest 代码/测试 -q
uv run ruff check 代码
uv run python 代码/生成定向筛选数据集.py --检查
uv run python 代码/生成代理模型训练输入.py --检查
git status --short
```

Expected: 全部测试与Ruff通过；数据发布检查通过；Git状态只包含本轮代码、配置、CSV/Markdown/JSON摘要，不包含模型二进制、缓存或日志。

- [ ] **Step 6: 更新入口并提交**

在根README新增`结果/代理模型/README.md`入口，在`结果/README.md`增加第三个业务目录`代理模型/`。

```powershell
git add pyproject.toml uv.lock README.md 代码 配置 结果/代理模型 结果/README.md
git commit -m "feat: train TPU three-target proxy models"
git push origin main
```

---

## 第一轮训练的明确边界

- 只预测当前980条高置信商业配方条件；另外5种商品级宏二醇等V0通过后再加入。
- 韧性和循环恢复可进入带不确定度的候选排序；热稳定若核心化学闭合仍为0，只发布辅助代理，不进入正式Pareto。
- 任何曲线点、循环点、方向和温度工况都不能增加化学配方数。
- 计算数据不能无条件与实验目标拼接；必须保留保真度、方法和来源层。
- 模型成功标准是来源族外推优于加权中位数基线，而不是训练集R²。

## 官方模型接口依据

- CatBoost的`fit`接口支持分类特征与`sample_weight`，用于配方家族、商品牌号和来源层级的混合特征训练：<https://catboost.ai/docs/en/concepts/python-reference_catboostregressor_fit>
- scikit-learn的`HistGradientBoostingRegressor`支持训练/验证样本权重和显式验证数据：<https://scikit-learn.org/stable/modules/generated/sklearn.ensemble.HistGradientBoostingRegressor.html>
- Optuna仅用于来源族分组验证内的有限超参数搜索；单目标调参可使用剪枝器：<https://optuna.readthedocs.io/en/stable/reference/pruners.html>
