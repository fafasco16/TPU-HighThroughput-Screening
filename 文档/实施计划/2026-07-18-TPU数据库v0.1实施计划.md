# TPU 数据库 v0.1 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` when available. This environment does not expose that skill, so use the built-in multi-agent delegation tools, keep file scopes disjoint, and have the primary agent review, test, and commit each integration point. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不改写原始文件的前提下，建立来源可追溯、许可证可门控、能处理标量与曲线数据的 TPU 数据库 v0.1，并推送至用户 GitHub 账户下的私人仓库。

**Architecture:** 现有 `sources` 迁移为浅层中文原始数据区；原始文件不纳入 Git。Python 管道负责 manifest、单位、曲线、适配器、QC 与 Parquet/DuckDB 快照；`@oai/artifact-tool` 负责生成 Excel 审核目录。首轮用四个垂直切片验证 chemical、property、curve 和 processing-curve 路径。

**Tech Stack:** Python 3.11、pandas、pyarrow、duckdb、openpyxl、PyYAML、pytest、pytest-cov；Node.js、`@oai/artifact-tool`；Git、GitHub CLI。

---

## 文件结构锁定

```text
E:\数据\TPU\
├─ 01_原始数据\
│  ├─ 基础数据\
│  ├─ 外部数据\
│  ├─ 代码仓库镜像\
│  └─ 仅供参考\
├─ 02_暂存数据\
├─ 03_规范数据\
├─ 04_派生数据\
├─ 05_数据库快照\
├─ 06_审核导出\
├─ 代码\
│  └─ 测试\
├─ 结构定义\
├─ 配置\
├─ 清单\
├─ 文档\
│  ├─ 设计规范\
│  ├─ 实施计划\
│  └─ 质量报告\
├─ README.md
├─ pyproject.toml
└─ .gitignore
```

第三方仓库和数据集内部目录/文件名保持原样，不改写上游结构；“中文命名”约束适用于本项目维护的目录。

### Task 1：建立中文浅层目录和 Git 安全边界

**Files:**
- Create: `README.md`
- Create: `.gitignore`
- Create: `pyproject.toml`
- Create: `代码/测试/conftest.py`
- Create: `配置/数据源.yaml`
- Move: `sources/*` → `01_原始数据/*`
- Move: `TPU_数据来源与研究路线台账.md` → `文档/TPU_数据来源与研究路线台账.md`
- Remove if empty: `sources/`, `docs/`

- [ ] **Step 1: 写目录结构验收测试**

Create `代码/测试/test_layout.py`:

```python
from pathlib import Path

ROOT = Path(__file__).resolve().parents[2]

def test_required_chinese_directories_exist():
    required = [
        "01_原始数据", "02_暂存数据", "03_规范数据", "04_派生数据",
        "05_数据库快照", "06_审核导出", "代码", "结构定义", "配置", "清单", "文档",
    ]
    assert all((ROOT / name).is_dir() for name in required)

def test_legacy_top_level_directories_removed():
    assert not (ROOT / "sources").exists()
    assert not (ROOT / "docs").exists()
```

- [ ] **Step 2: 运行测试并确认失败**

Run:

```powershell
.\.venv\Scripts\python.exe -m pytest 代码\测试\test_layout.py -q
```

Expected: FAIL，缺少中文目录或仍存在旧目录。

- [ ] **Step 3: 迁移目录并写安全配置**

`.gitignore` 必须至少包含：

```gitignore
.venv/
__pycache__/
.pytest_cache/
.coverage
htmlcov/
01_原始数据/**
!01_原始数据/README.md
02_暂存数据/**
!02_暂存数据/README.md
03_规范数据/**/*.parquet
04_派生数据/**/*.parquet
05_数据库快照/**/*.duckdb
06_审核导出/*.xlsx
*.zip
*.pdf
*.docx
```

每个被忽略的数据目录保留一个可提交的 `README.md`，说明本地用途、许可边界和重建命令。

`pyproject.toml` 固定为：

```toml
[project]
name = "tpu-high-throughput-screening"
version = "0.1.0"
requires-python = ">=3.11,<3.13"
dependencies = [
  "pandas>=2.2,<3",
  "pyarrow>=17,<22",
  "duckdb>=1.2,<2",
  "openpyxl>=3.1,<4",
  "PyYAML>=6,<7",
]

[project.optional-dependencies]
dev = ["pytest>=8,<10", "pytest-cov>=5,<8"]

[tool.pytest.ini_options]
testpaths = ["代码/测试"]
addopts = "-ra"

[tool.coverage.run]
source = ["代码"]
omit = ["代码/测试/*"]

[tool.coverage.report]
fail_under = 90
show_missing = true
```

`代码/测试/conftest.py` 固定把 `代码/` 加入模块搜索路径：

```python
import sys
from pathlib import Path

CODE_DIR = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(CODE_DIR))
```

- [ ] **Step 4: 初始化 Python 环境与有效 Git 仓库**

Run:

```powershell
uv venv .venv --python 3.11
uv pip install --python .venv\Scripts\python.exe pandas pyarrow duckdb openpyxl pyyaml pytest pytest-cov
git init -b main
git config core.quotepath false
```

Expected: `.venv` 可用，`git status --short --branch` 显示 `## No commits yet on main`，原始数据不出现在待提交清单中。

- [ ] **Step 5: 运行目录测试并提交**

Run:

```powershell
.\.venv\Scripts\python.exe -m pytest 代码\测试\test_layout.py -q
git check-ignore -v 01_原始数据\外部数据\TPU_HBond_2021_Source_Main.xlsx
git add .gitignore README.md pyproject.toml 配置 结构定义 代码 文档 01_原始数据\README.md 02_暂存数据\README.md 03_规范数据\README.md 04_派生数据\README.md 05_数据库快照\README.md 06_审核导出\README.md 清单\README.md
git commit -m "chore: 建立TPU数据库中文分层结构"
```

Expected: 测试 PASS；`git check-ignore` 命中 `.gitignore`；首次提交不含大数据文件。

### Task 2：实现稳定 ID、manifest、schema 和许可证门控

**Files:**
- Create: `代码/ids.py`
- Create: `代码/manifest.py`
- Create: `代码/schema.py`
- Create: `代码/licensing.py`
- Create: `结构定义/v0.1字段字典.yaml`
- Create: `结构定义/v0.1枚举.yaml`
- Create: `代码/测试/test_ids.py`
- Create: `代码/测试/test_manifest.py`
- Create: `代码/测试/test_licensing.py`

- [ ] **Step 1: 写稳定 ID 与许可证失败测试**

```python
from ids import stable_id
from licensing import may_publish

def test_stable_id_is_deterministic_and_namespaced():
    assert stable_id("chemical", "CCO") == stable_id("chemical", "CCO")
    assert stable_id("chemical", "CCO").startswith("chemical_")

def test_unknown_and_nd_licenses_are_blocked():
    assert not may_publish("UNKNOWN", derivatives_allowed=None, redistribution_allowed=None)
    assert not may_publish("CC-BY-NC-ND-4.0", derivatives_allowed=False, redistribution_allowed=True)

def test_cc_by_is_publishable():
    assert may_publish("CC-BY-4.0", derivatives_allowed=True, redistribution_allowed=True)
```

- [ ] **Step 2: 运行测试并确认失败**

Run:

```powershell
.\.venv\Scripts\python.exe -m pytest 代码\测试\test_ids.py 代码\测试\test_licensing.py -q
```

Expected: FAIL，模块尚不存在。

- [ ] **Step 3: 实现最小稳定 ID 和门控**

`代码/ids.py`:

```python
import hashlib
import json

def stable_id(namespace: str, *parts: object) -> str:
    payload = json.dumps(parts, ensure_ascii=False, separators=(",", ":"), sort_keys=True)
    digest = hashlib.sha256(payload.encode("utf-8")).hexdigest()[:16]
    return f"{namespace}_{digest}"
```

`代码/licensing.py`:

```python
def may_publish(license_spdx: str, derivatives_allowed: bool | None,
                redistribution_allowed: bool | None) -> bool:
    return bool(
        license_spdx not in {"", "UNKNOWN", "NOASSERTION"}
        and derivatives_allowed is True
        and redistribution_allowed is True
        and "-ND-" not in license_spdx
    )
```

- [ ] **Step 4: 实现文件 manifest**

`代码/manifest.py` 必须流式计算 SHA-256，输出列固定为：

```python
MANIFEST_COLUMNS = [
    "source_id", "source_file_id", "raw_path", "original_filename", "size_bytes",
    "sha256", "doi", "url", "accessed_at", "license_spdx", "derivatives_allowed",
    "redistribution_allowed", "evidence_grade", "material_scope", "status", "notes",
]
```

扫描时排除所有嵌套 `.git` 对象，路径统一相对项目根目录并使用 `/`。

- [ ] **Step 5: 写 manifest fixture 测试并通过**

测试必须验证：同一文件哈希稳定、修改一个字节后哈希变化、`.git` 文件不进入 manifest、所有路径为相对路径。

Run:

```powershell
.\.venv\Scripts\python.exe -m pytest 代码\测试\test_ids.py 代码\测试\test_manifest.py 代码\测试\test_licensing.py --cov=代码 --cov-report=term-missing --cov-fail-under=90
```

Expected: PASS，覆盖率不低于 90%。

- [ ] **Step 6: 提交核心基础设施**

```powershell
git add 代码 结构定义
git commit -m "feat: 添加来源清单与许可证门控"
```

### Task 3：实现单位与曲线指标模块

**Files:**
- Create: `代码/units.py`
- Create: `代码/curves.py`
- Create: `代码/测试/test_units.py`
- Create: `代码/测试/test_curves.py`

- [ ] **Step 1: 写单位和曲线失败测试**

```python
import numpy as np
from units import strain_to_fraction, stress_to_mpa, temperature_to_k
from curves import tensile_metrics, cycle_metrics

def test_core_unit_conversions():
    assert strain_to_fraction(100.0, "%") == 1.0
    assert stress_to_mpa(1_000_000.0, "Pa") == 1.0
    assert temperature_to_k(25.0, "degC") == 298.15

def test_toughness_units_mpa_times_fraction_equals_mj_per_m3():
    result = tensile_metrics(np.array([0.0, 1.0]), np.array([0.0, 10.0]))
    assert result["toughness_mj_m3"] == 5.0
    assert result["tensile_strength_mpa"] == 10.0
    assert result["elongation_at_break"] == 1.0

def test_cycle_hysteresis_is_loading_minus_unloading_area():
    result = cycle_metrics(
        load_strain=np.array([0.0, 1.0]), load_stress=np.array([0.0, 10.0]),
        unload_strain=np.array([1.0, 0.0]), unload_stress=np.array([10.0, 0.0]),
    )
    assert result["hysteresis_mj_m3"] == 0.0
```

- [ ] **Step 2: 实现显式单位白名单**

未知单位必须抛出 `ValueError`，禁止猜测；转换后同时保留原始值/单位列。

- [ ] **Step 3: 实现曲线积分与输入验证**

使用梯形积分；检查长度相等、有限值、点数至少为 2；输出值、单位、算法版本、积分点数和警告列表。

- [ ] **Step 4: 运行覆盖率测试并提交**

```powershell
.\.venv\Scripts\python.exe -m pytest 代码\测试\test_units.py 代码\测试\test_curves.py --cov=代码 --cov-report=term-missing --cov-fail-under=90
git add 代码\units.py 代码\curves.py 代码\测试
git commit -m "feat: 添加单位规范化与曲线指标"
```

### Task 4：实现四个垂直切片适配器

**Files:**
- Create: `代码/adapter_smipoly.py`
- Create: `代码/adapter_pue326.py`
- Create: `代码/adapter_hbond.py`
- Create: `代码/adapter_viscosity.py`
- Create: `代码/测试/test_adapters.py`
- Create: `代码/测试/夹具/`

- [ ] **Step 1: 为四类源格式建立小型夹具**

夹具只复制最小、可公开、足以验证列结构的若干行/点；每个夹具附来源和生成说明，不放入受限完整数据。

- [ ] **Step 2: 写适配器契约失败测试**

每个适配器必须输出：`record_id, source_id, source_file_id, source_locator, extraction_method, fidelity, schema_version`；源 header/sheet 指纹变化时 fail-fast。

- [ ] **Step 3: 实现 SMiPoly chemical 适配器**

输入：`01_原始数据/基础数据/smipoly_monomers.csv`。输出 chemical staging 表；保留原始 SMILES、规范化状态、重复组和 `role_status=unclassified`，不虚构官能度或 TPU 角色。

- [ ] **Step 4: 实现 PUE 326 标量适配器和谱系锁**

输入：DQ 原始 `PUE.csv`。DQ、MatImpute 及其缺失率变体使用共同 `lineage_family=pue643_family`，同源记录生成相同 `split_group`，防止衍生文件跨训练/测试集。

- [ ] **Step 5: 实现 TPU 氢键 XLSX 曲线适配器**

输入：`TPU_HBond_2021_Source_Main.xlsx`。用 sheet 名与列组显式映射到 `curve`/`curve_point`；未识别 sheet 进入审计报告，不静默跳过。

- [ ] **Step 6: 实现预聚体温度—黏度适配器**

输入：`ViscTempData.xlsx`。输出曲线类型 `viscosity_temperature`，温度规范为 K、黏度为 Pa·s，原始单位并存。

- [ ] **Step 7: 运行适配器测试并提交**

```powershell
.\.venv\Scripts\python.exe -m pytest 代码\测试\test_adapters.py --cov=代码 --cov-report=term-missing --cov-fail-under=90
git add 代码 结构定义
git commit -m "feat: 添加首批四类数据适配器"
```

### Task 5：实现 QC、Parquet 和 DuckDB 快照

**Files:**
- Create: `代码/qc.py`
- Create: `代码/snapshot.py`
- Create: `代码/run_pipeline.py`
- Create: `代码/测试/test_qc.py`
- Create: `代码/测试/test_snapshot.py`

- [ ] **Step 1: 写 QC 与发布门失败测试**

测试覆盖：重复 PK、孤儿 FK、缺 provenance、未知单位、曲线 point_index 重复、非有限数值、许可泄漏、同 lineage 跨 split。

- [ ] **Step 2: 实现 fail-fast 与异常表**

结构性错误中止构建；科学异常进入 `04_派生数据/质量异常.parquet`，保留规则 ID、严重级别、记录 ID、源定位和说明。

- [ ] **Step 3: 实现确定性快照**

Parquet 写入前固定列序、行序和 dtype；DuckDB 创建只读分析表/视图；`清单/快照_v0.1.json` 记录 schema、代码提交、环境、输入/输出哈希、行数和时间。

- [ ] **Step 4: 实现统一 CLI**

Run:

```powershell
.\.venv\Scripts\python.exe 代码\run_pipeline.py manifest
.\.venv\Scripts\python.exe 代码\run_pipeline.py build --version v0.1.0
.\.venv\Scripts\python.exe 代码\run_pipeline.py qc --version v0.1.0
```

- [ ] **Step 5: 两次 clean build 哈希一致性测试**

在两个临时目录运行 mini fixture，全套 Parquet 输出哈希必须相同。

- [ ] **Step 6: 全测试与提交**

```powershell
.\.venv\Scripts\python.exe -m pytest 代码\测试 --cov=代码 --cov-report=term-missing --cov-report=html --cov-fail-under=90
git add 代码 结构定义 配置
git commit -m "feat: 构建可复现数据库快照与质量门"
```

### Task 6：生成 v0.1 本地数据库和质量报告

**Files:**
- Create: `清单/来源清单.csv`
- Create: `清单/快照_v0.1.json`
- Create: `03_规范数据/*.parquet`
- Create: `04_派生数据/*.parquet`
- Create: `05_数据库快照/TPU数据库_v0.1.duckdb`
- Create: `文档/质量报告/v0.1质量报告.md`
- Modify: `文档/TPU_数据来源与研究路线台账.md`

- [ ] **Step 1: 运行全量 manifest**

验证排除嵌套 `.git`；每个原始文件有相对路径、字节数和 SHA-256。

- [ ] **Step 2: 构建四个垂直切片**

完整源文件只读；输出 staging、curated、derived 和快照。

- [ ] **Step 3: 运行全量 QC**

必须报告：表行数、缺失率、单位状态、重复、PK/FK、来源覆盖、材料作用域、谱系组、许可证和发布门结果。

- [ ] **Step 4: 更新论文引用台账**

记录数据库版本、数据源、适配器状态、文件哈希和正式参考文献；不得把未取得的 ScienceDB/PUE-643 原数据写成已入库。

- [ ] **Step 5: 提交可公开的小型清单与报告**

```powershell
git add 清单 文档
git commit -m "data: 发布TPU数据库v0.1清单与质量报告"
```

大 Parquet、DuckDB、XLSX 和原始数据由 `.gitignore` 保持本地，不进入 GitHub。

### Task 7：生成并视觉验证 Excel 审核目录

**Files:**
- Create: `代码/build_catalog.mjs`
- Create: `06_审核导出/TPU数据库_v0.1_审核目录.xlsx`
- Create temporary preview PNGs outside final output links

- [ ] **Step 1: 创建独立 Node 工作目录依赖链接**

在临时构建目录创建指向加载器提供的 `node_modules` 的 Windows junction，不修改依赖目录。

- [ ] **Step 2: 用 `@oai/artifact-tool` 创建审核簿**

工作表固定为：`总览`、`数据来源`、`字段字典`、`数据覆盖`、`质量问题`、`许可证`、`构建信息`。URL 使用纯文本单元格；表头冻结、筛选开启、单位列明确、状态用条件格式。

- [ ] **Step 3: 公式与内容检查**

用 `workbook.inspect` 检查关键范围，扫描 `#REF!|#DIV/0!|#VALUE!|#NAME?|#N/A`。

- [ ] **Step 4: 逐 sheet 渲染并查看**

每个 sheet 至少一次可视化渲染；修复截断、空白页、不可读列宽和失效条件格式。

- [ ] **Step 5: 导出并提交构建器**

```powershell
git add 代码\build_catalog.mjs
git commit -m "feat: 添加数据库审核目录生成器"
```

最终 XLSX 保持本地并由 README 给出重建命令。

### Task 8：创建 GitHub 私人仓库并推送

**Files:**
- Modify: `README.md`
- Verify only: Git index and GitHub repository

- [ ] **Step 1: 推送前敏感与大文件审计**

Run:

```powershell
git status --short
git ls-files
git ls-files | ForEach-Object { if ((Get-Item -LiteralPath $_).Length -gt 20MB) { $_ } }
git grep -n -I -E "(gho_|github_pat_|api[_-]?key|secret|token)"
```

Expected: 没有原始数据、受限附件、密钥或超过 20 MB 的已跟踪文件。

- [ ] **Step 2: 创建私人仓库**

Repository: `fafasco16/TPU-HighThroughput-Screening`

Run:

```powershell
gh repo create fafasco16/TPU-HighThroughput-Screening --private --source . --remote origin --description "TPU高通量筛选数据库、数据管道与多保真研究工作流"
git push -u origin main
```

若同名仓库已存在，则先验证其 owner/visibility，再添加为 `origin`，不得覆盖未知仓库历史。

- [ ] **Step 3: 远程验证**

```powershell
gh repo view fafasco16/TPU-HighThroughput-Screening --json nameWithOwner,visibility,url,defaultBranchRef
git status --short --branch
```

Expected: `visibility=PRIVATE`，默认分支 `main`，本地与远程同步，工作区无未解释改动。

## 最终验收

- [ ] 中文项目目录完整，层级不超过三层；第三方内部结构例外并写入 README。
- [ ] 原始数据内容未改写，manifest 哈希完整。
- [ ] 四种垂直切片端到端构建成功。
- [ ] 单位、曲线、来源、谱系和许可证测试覆盖率 ≥90%。
- [ ] PK 重复、孤儿 FK、缺 provenance 和 public 许可泄漏均为 0。
- [ ] DuckDB/Parquet 可查询，Excel 审核目录无公式错误且视觉验证通过。
- [ ] GitHub 仓库为 PRIVATE，未推送原始大文件、受限附件或密钥。
